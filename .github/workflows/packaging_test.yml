name: "Packaging Test"

on:
  push:
    branches:
      - main
  pull_request: {}
jobs:
  packaging:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Install uv
        uses: astral-sh/setup-uv@v7
      - name: Test packaging
        run: |
          uv build
          uv run twine check dist/*
