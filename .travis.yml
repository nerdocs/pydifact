language: python
python:
  - 3.10
  - 3.11
  - 3.12
  - 3.13
before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
install:
  - pip install .
script: pytest # run tests
